<Page
  x:Class="Source.BasicItemsControlPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Source"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="BasicItemsControlPage"
  d:DesignHeight="450"
  d:DesignWidth="800"
  Loaded="Page_Loaded"
  mc:Ignorable="d">
  <StackPanel>
    <TextBlock Text="Colors" />
    <ComboBox x:Name="colorsComboBox">
      <ComboBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal">
            <Rectangle
              Width="16"
              Margin="0,0,4,0"
              Fill="{Binding Name}" />
            <TextBlock Text="{Binding Name}" />
          </StackPanel>
        </DataTemplate>
      </ComboBox.ItemTemplate>
    </ComboBox>

    <TextBlock Text="Fonts" />
    <ComboBox x:Name="fontsComboBox" ItemsSource="{x:Static Fonts.SystemFontFamilies}">
      <ComboBox.ItemTemplate>
        <DataTemplate>
          <StackPanel>
            <TextBlock
              Margin="0,4"
              VerticalAlignment="Center"
              Text="{Binding}" />
            <TextBlock
              Margin="0,0,0,2"
              VerticalAlignment="Center"
              FontFamily="{Binding}"
              Text="The quick brown fox jumps over the lazy dog." />
          </StackPanel>
        </DataTemplate>
      </ComboBox.ItemTemplate>
    </ComboBox>
  </StackPanel>
</Page>
